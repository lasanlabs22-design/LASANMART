/**
 * User video uploads stay off until there's a moderation step in the
 * console. Everything underneath still works — flip this to true to
 * open it up, no other changes needed.
 */
export const VIBES_UNLOCKED = false;
